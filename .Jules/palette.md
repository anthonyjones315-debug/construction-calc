## 2025-05-14 - [Centralized UX Patterns]
**Learning:** UX patterns like "Copied" feedback should be implemented within the component itself (e.g., `ProResult`) rather than at the page level. This ensures consistency across all instances of the component and simplifies the page logic.
**Action:** When adding interaction feedback, always check if the logic belongs to a shared UI component.
