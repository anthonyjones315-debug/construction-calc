/**
 * Field Notes article metadata and content — Regional Authority pack.
 * Single source for hub and [slug] pages; re-exports from shared data layer.
 */

export {
  FIELD_NOTES,
  getFieldNoteBySlug,
  type FieldNote,
  type FieldNoteToolLink,
} from "@/data/field-notes";
